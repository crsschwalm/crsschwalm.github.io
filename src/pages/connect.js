import React from 'react';

export const Connect = ({ className = 'connect' }) => (
    <div className={className}>
        <div className={`${className} hero`}>
            <section>
                Connect
            </section>
        </div>
        <a href="mailto:crsschwalm@gmail.com?subject=I noticed your portfolio Site">Email Me</a>
        Connect
    </div>)